<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejs</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-blue-300 via-purple-500 to-green-500">

    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white rounded-lg shadow-md p-8 w-80">

            <h1 class="text-4xl font-bold mb-4 text-center text-gray-800">Your Tasks For</h1>
            <h2 class="text-xl font-bold mb-4 text-center text-gray-800"> <%= calendar %>: </h2>
            
            <h3 class="text-lg font-semibold mb-2 text-gray-800">Check</h3> <!-- Added "Check" heading -->
            <ul class="space-y-2 text-lg text-gray-700">
                <% for ( var i = 0 ; i < toDo.length ; i++ ) { %>
                    <label class="flex items-center">
                        <input type="checkbox" class="rounded-full form-checkbox h-5 w-5 mt-1" onchange="strikeThrough(this, <%= g %>)" />
                        <span class="ml-2 <%= toDo[i].checked ? 'line-through' : '' %>"><%= toDo[i].task %></span>
                    </label>
                <% } %>
            </ul>

            <form action="/" method="post" class="flex items-center justify-center mt-4">
                <input type="text" autocomplete="off" placeholder="Add new task" name="newTask" class="border rounded px-4 py-2 w-48 focus:outline-none focus:ring focus:border-blue-300">
                <button type="submit" class="bg-blue-500 text-white px-4 py-2 ml-2 rounded-full hover:bg-blue-600">+</button>
            </form>
        </div>
    </div>

    <script>
        function strikeThrough(checkbox, index) {
            const taskElement = checkbox.nextElementSibling;
            taskElement.classList.toggle('line-through');
        }
    </script>
    <script src=".public/index.js"></script>
</body>
</html>
