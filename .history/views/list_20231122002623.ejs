<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejs</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-blue-300 via-purple-500 to-green-500">

    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white rounded-lg shadow-md p-8 w-80">
            <h1 class="text-4xl font-bold mb-4 text-center text-gray-800">Your Tasks</h1>
            <h2 class="text-xl font-bold mb-4 text-center text-gray-800"> <%= calendar %> </h2>

            <ul class="space-y-2 text-lg text-gray-700">
                <% for ( var i = 0 ; i < toDo.length ; i++ ) { %>
                    <div class="flex flex-row">
                    <input type="checkbox" class=" mx-2 my-1">
                    <p class="flex items-center justify-between">
                        <span><%= toDo[i] %></span>
                        <button class="text-red-500 hover:text-red-700"><svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10 1a1 1 0 00-1 1v13H6a1 1 0 100 2h8a1 1 0 100-2h-3V2a1 1 0 00-1-1zm-3 4v11h6V5H7z" clip-rule="evenodd"/></svg></button>
                    </p>
                </div>
                <% } %>
            </ul>

            <form action="/" method="post" class="flex items-center justify-center mt-4">
                <input type="text" placeholder="Add new task" name="newTask" class="border rounded px-4 py-2 w-48 focus:outline-none focus:ring focus:border-blue-300">
                <button type="submit" class="bg-blue-500 text-white px-6 py-2 ml-2 rounded hover:bg-blue-600">+</button>
            </form>
        </div>
    </div>

    <script src="../public/index.js"></script>
</body>
</html>
