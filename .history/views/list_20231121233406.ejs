<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejs</title>
    <!-- Include Tailwind CSS from CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-4">
    <div class="main items-center justify-center align-middle ">

    <h1 class="text-2xl font-bold mb-4">To-Do List</h1>

    <ul class="list-disc pl-4">
        <% for (var i = 0; i < toDo.length; i++) { %>
            <li class="mb-2"><%= toDo[i] %></li>
        <% } %>
    </ul>

    <form action="/" method="post" class="mt-4">
        <input type="text" placeholder="Add new task" name="newTask" class="border rounded py-2 px-3 mr-2">
        <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded">Add</button>
    </form>

</div>
    <script src="../src/index.js"></script>
</body>
</html>
